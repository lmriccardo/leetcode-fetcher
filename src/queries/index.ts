import userql from './profile_page'
import problemsetql from './problemset_page';
import problem_solve_ql from './problem_solve_page';

const queries = { user: userql, problemset: problemsetql, solveql: problem_solve_ql };

export default queries;